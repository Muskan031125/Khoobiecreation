# Redirect everything to public/ so URLs stay clean.
# In production the document root should point directly at /public.
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
